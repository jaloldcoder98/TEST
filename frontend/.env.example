NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1

# Bot username without the @ (e.g. gym_ai_bot). The landing page is the only thing that uses it:
# with no web sign-up left (docs/DECISIONS.md D-10), its job is to send people to the bot. Unset,
# the page says so in words instead of linking nowhere.
NEXT_PUBLIC_TELEGRAM_BOT_USERNAME=
